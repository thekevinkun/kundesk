// Drizzle inferred types — populated after schema is defined in lib/db/schema.ts
// Pattern: export type OrgSelect = typeof orgs.$inferSelect
// Pattern: export type OrgInsert = typeof orgs.$inferInsert
// This file is intentionally sparse until Step 9 (Neon + Drizzle setup)

export {};
